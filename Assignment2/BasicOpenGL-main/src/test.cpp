/*
#define GLM_ENABLE_EXPERIMENTAL
#include <glm/glm.hpp>
#include <glm/gtx/string_cast.hpp> 

#include <iostream>
#include "SceneReader.h"
#include "Scene.h"

int main() {
    std::string filename = "sample_scene.txt";

    
        // Read the scene using the readScene method
        SceneReader reader;
        Scene* scene = reader.readScene(filename);

        reader.generateRays(scene);
    

    
    return 0;
}
*/
